#include "main.h"
/**
 * more_numbers - a function that pr
*/
void more_numbers(void);
{
    int i, j = 0;
    for ()
        for (i = 48; i < 58; i++)
        {
            if (j < 58)
            {
                _putchar(i);
                j++;
            }
            if (j>)
        }
    putchar('\n');
}