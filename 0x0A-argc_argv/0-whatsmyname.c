#include <stdio.h>
#include <stdlib.h>

/* argc = argument count
 * argv = value at each count of argument
 * Let's do this!
 */
   int main(int argc, char **argv[]) {
	
	printf("%s", argv[0]);

	return 0;
}

